# Simulateur Donation & Succession

Application Windows (WPF .NET 8) visant à simuler des scénarios de donation et successions avec stockage local JSON (sans Excel, sans base).

Stockage : Documents\SimulateurDonationSuccession\appstate.json`r

Run :
    dotnet run --project .\src\NotaireSimu.App\NotaireSimu.App.csproj

